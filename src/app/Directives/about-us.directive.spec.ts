import { AboutUsDirective } from './about-us.directive';

describe('AboutUsDirective', () => {
  it('should create an instance', () => {
    const directive = new AboutUsDirective();
    expect(directive).toBeTruthy();
  });
});
